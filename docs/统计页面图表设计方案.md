# 统计页面图表设计方案

> 基于后端统计接口设计的前端图表展示方案
> 
> 参考文档：`siae/services/siae-user/docs/统计接口设计.md`
> 
> 创建日期：2024-11-17

---

## 📋 目录

1. [页面结构概览](#页面结构概览)
2. [成员统计页面](#成员统计页面)
3. [获奖统计页面](#获奖统计页面)
4. [综合仪表盘首页](#综合仪表盘首页)
5. [技术实现建议](#技术实现建议)
6. [开发优先级](#开发优先级)

---

## 页面结构概览

### 路由规划

```
/dashboard                          # 综合仪表盘（首页）
├── 快速概览卡片
├── 成员与获奖趋势
└── 各维度分布快照

/members                            # 成员管理模块
├── /list                          # 成员列表（CRUD）
└── /statistics                    # 成员统计分析
    ├── 成员概览卡片
    ├── 部门分布 + 职位分布
    ├── 性别/年级/专业分布
    └── 入会趋势

/awards                             # 获奖管理模块
├── /list                          # 获奖列表（CRUD）
└── /statistics                    # 获奖统计分析
    ├── 获奖概览卡片
    ├── 等级分布 + 类型分布
    ├── 获奖趋势
    └── 获奖排行榜
```

---

## 成员统计页面

**路由**: `/members/statistics`

### 1. 顶部概览卡片区

**接口**: `GET /user/statistics/overview`

**图表类型**: 数字卡片组（6个卡片）

**展示内容**:

| 卡片 | 数据字段 | 说明 |
|------|----------|------|
| 总用户数 | `totalUsers` | 所有未删除的用户总数 |
| 启用/禁用用户 | `enabledUsers` / `disabledUsers` | 账号状态统计 |
| 正式成员 | `formalMembers` | 已转正的成员数 |
| 候选成员 | `candidateMembers` | 候选成员数 |
| 本月新增 | `newUsersThisMonth` | 本月创建的用户数 |
| 本年新增 | `newUsersThisYear` | 本年创建的用户数 |

**设计要点**:
- 每个卡片包含：图标、数字、标题、简短描述
- 使用不同颜色区分不同类型的数据
- 可添加环比/同比增长百分比（需要额外接口支持）

---

### 2. 第一行：部门与职位分布

#### 左侧：部门分布（占2/3宽度）

**接口**: `GET /user/statistics/departments`

**图表类型**: 分组柱状图（Grouped Bar Chart）

**配置**:
- **X轴**: 部门名称 (`departmentName`)
- **Y轴**: 人数
- **数据系列**:
  - 总人数 (`totalMembers`) - 蓝色
  - 正式成员 (`formalMembers`) - 绿色
  - 候选成员 (`candidateMembers`) - 橙色
- **交互**: 点击柱子可跳转到该部门的成员列表

**示例数据**:
```json
[
  {
    "departmentName": "技术部",
    "totalMembers": 45,
    "formalMembers": 30,
    "candidateMembers": 15
  }
]
```

#### 右侧：职位分布（占1/3宽度）

**接口**: `GET /user/statistics/positions`

**图表类型**: 环形图（Doughnut Chart）

**配置**:
- **数据**: 职位名称 (`positionName`) 和人数 (`count`)
- **中心文字**: 显示总职位人数
- **颜色**: 使用渐变色系
- **交互**: 点击扇区可查看该职位的成员列表

---

### 3. 第二行：人口统计分析

#### 左侧：性别分布（占1/3宽度）

**接口**: `GET /user/statistics/demographics/gender`

**图表类型**: 饼图（Pie Chart）

**配置**:
- **数据**: 性别名称 (`genderName`) 和人数 (`count`)
- **颜色**: 男-蓝色、女-粉色、未知-灰色
- **标签**: 显示百分比 (`percentage`)

#### 中间：年级分布（占1/3宽度）

**接口**: `GET /user/statistics/demographics/grade`

**图表类型**: 柱状图（Bar Chart）

**配置**:
- **X轴**: 年级 (`grade`)，如"2024级"
- **Y轴**: 人数 (`count`)
- **排序**: 按年级降序排列（最新年级在前）
- **颜色**: 渐变色

#### 右侧：专业分布（占1/3宽度）

**接口**: `GET /user/statistics/demographics/major`

**图表类型**: 横向条形图（Horizontal Bar Chart）

**配置**:
- **Y轴**: 专业名称 (`majorName`)
- **X轴**: 人数 (`count`)
- **显示**: TOP 10专业
- **滚动**: 如果专业超过10个，支持滚动查看
- **交互**: 点击可查看该专业的成员列表

---

### 4. 第三行：入会趋势（全宽）

**接口**: `GET /user/statistics/membership/trend`

**图表类型**: 双折线图 + 面积填充（Line Chart with Area）

**配置**:
- **X轴**: 时间 (`period`)，格式：YYYY-MM 或 YYYY
- **Y轴**: 人数
- **数据系列**:
  - 候选成员入会数 (`candidateCount`) - 蓝色线 + 浅蓝色填充
  - 转正人数 (`formalCount`) - 绿色线 + 浅绿色填充
- **参数控制**:
  - 周期切换：按月 / 按年
  - 时间范围：最近6个月、12个月、24个月
- **交互**: 
  - 鼠标悬停显示具体数值
  - 可以缩放和拖拽查看不同时间段

**请求参数**:
```typescript
{
  period: 'month' | 'year',  // 默认 'month'
  limit: 12                   // 默认 12
}
```

---

## 获奖统计页面

**路由**: `/awards/statistics`

### 1. 顶部概览卡片区

**接口**: `GET /user/statistics/awards/overview`

**图表类型**: 数字卡片组（5个卡片）

**展示内容**:

| 卡片 | 数据字段 | 说明 |
|------|----------|------|
| 总获奖数 | `totalAwards` | 所有未删除的获奖记录总数 |
| 本年获奖 | `thisYearAwards` | 本年度获奖数 |
| 本月获奖 | `thisMonthAwards` | 本月获奖数 |
| 团队获奖 | `teamAwards` | 团队获奖数 |
| 个人获奖 | `individualAwards` | 个人获奖数 |

---

### 2. 第一行：获奖分布

#### 左侧：按等级分布（占1/2宽度）

**接口**: `GET /user/statistics/awards/by-level`

**图表类型**: 环形图（Doughnut Chart）

**配置**:
- **数据**: 等级名称 (`levelName`) 和数量 (`count`)
- **颜色**: 渐变色系（金色→银色→铜色）
  - 国家级：金色 (#FFD700)
  - 省级：银色 (#C0C0C0)
  - 校级：铜色 (#CD7F32)
- **标签**: 显示数量和占比
- **中心文字**: 显示总获奖数

#### 右侧：按类型分布（占1/2宽度）

**接口**: `GET /user/statistics/awards/by-type`

**图表类型**: 玫瑰图/南丁格尔图（Rose Chart / Nightingale Chart）

**配置**:
- **数据**: 类型名称 (`typeName`) 和数量 (`count`)
- **半径**: 表示数量大小
- **角度**: 表示占比
- **交互**: 点击可查看该类型的获奖列表

---

### 3. 第二行：获奖趋势（全宽）

**接口**: `GET /user/statistics/awards/trend`

**图表类型**: 面积图 + 折线图（Area Chart with Line）

**配置**:
- **X轴**: 时间 (`period`)，格式：YYYY-MM 或 YYYY
- **Y轴**: 获奖数量 (`count`)
- **面积填充**: 显示累积效果
- **平均线**: 添加虚线显示平均值
- **参数控制**:
  - 周期切换：按月 / 按年
  - 时间范围：最近12个月、24个月、36个月
- **数据标签**: 在关键点显示具体数值
- **交互**: 
  - 鼠标悬停显示详细信息
  - 支持缩放和拖拽

**请求参数**:
```typescript
{
  period: 'month' | 'year',  // 默认 'month'
  limit: 12                   // 默认 12
}
```

---

### 4. 第三行：获奖排行榜（全宽）

**接口**: `GET /user/statistics/awards/top`

**图表类型**: 排行榜列表 + 横向堆叠条形图

**布局结构**:
```
┌─────────────────────────────────────────────────────────────┐
│ 排名 │ 头像 │ 姓名/用户名 │ ████████████████ │ 总获奖数 │
│  🥇  │  👤  │  张三       │ ■■■■■■■■■■■■■■■ │   15    │
│      │      │  zhangsan   │ 国家3 省5 校7    │         │
├─────────────────────────────────────────────────────────────┤
│  🥈  │  👤  │  李四       │ ■■■■■■■■■■■■    │   12    │
│      │      │  lisi       │ 国家2 省4 校6    │         │
└─────────────────────────────────────────────────────────────┘
```

**配置**:
- **排名徽章**: 前三名显示金银铜奖牌图标
- **头像**: 显示用户头像 (`avatarUrl`)
- **姓名**: 显示真实姓名 (`realName`) 和用户名 (`username`)
- **横向堆叠条形图**:
  - 红色：国家级 (`nationalCount`)
  - 橙色：省级 (`provincialCount`)
  - 蓝色：校级 (`schoolCount`)
- **总获奖数**: 显示在最右侧 (`awardCount`)
- **参数控制**: 支持切换显示数量（TOP 10/20/50）
- **交互**: 点击行可查看该成员的获奖详情

**请求参数**:
```typescript
{
  limit: 10  // 默认 10，最大 100
}
```

---

## 综合仪表盘首页

**路由**: `/dashboard`

**说明**: 用户登录后看到的第一个页面，展示最核心的数据

### 1. 顶部：快速概览（4个大卡片）

**数据来源**:
- `GET /user/statistics/overview` - 成员数据
- `GET /user/statistics/awards/overview` - 获奖数据

**展示内容**:

| 卡片 | 数据来源 | 字段 |
|------|----------|------|
| 总成员数 | 成员概览 | `totalUsers` |
| 正式成员数 | 成员概览 | `formalMembers` |
| 总获奖数 | 获奖概览 | `totalAwards` |
| 本月新增获奖 | 获奖概览 | `thisMonthAwards` |

**设计要点**:
- 大号数字显示
- 带图标和颜色区分
- 显示环比变化（需要额外接口支持）

---

### 2. 第一行：趋势对比

#### 左侧：成员入会趋势（占60%宽度）

**接口**: `GET /user/statistics/membership/trend?period=month&limit=12`

**图表类型**: 双折线图

**配置**:
- 标题："成员入会趋势"
- 显示最近12个月的候选入会和转正趋势
- 简化版，不显示面积填充

#### 右侧：获奖趋势（占40%宽度）

**接口**: `GET /user/statistics/awards/trend?period=month&limit=12`

**图表类型**: 迷你折线图

**配置**:
- 标题："获奖趋势"
- 显示最近12个月的获奖趋势
- 简化版，只显示折线

---

### 3. 第二行：分布概览

#### 左侧：部门分布（占33%宽度）

**接口**: `GET /user/statistics/departments`

**图表类型**: 横向条形图（TOP 5）

**配置**:
- 标题："部门分布"
- 只显示前5个部门的总人数
- 底部有"查看更多"按钮，点击跳转到 `/members/statistics`

#### 中间：获奖等级分布（占33%宽度）

**接口**: `GET /user/statistics/awards/by-level`

**图表类型**: 环形图

**配置**:
- 标题："获奖等级分布"
- 中心显示总获奖数
- 使用金银铜配色

#### 右侧：获奖排行榜（占33%宽度）

**接口**: `GET /user/statistics/awards/top?limit=5`

**图表类型**: 迷你排行榜（TOP 5）

**配置**:
- 标题："获奖排行榜"
- 只显示前5名
- 简化版：排名 + 头像 + 姓名 + 获奖数
- 底部有"查看更多"按钮，点击跳转到 `/awards/statistics`

---

### 4. 第三行：人口统计快照

**布局**: 4个小型图表，每个占25%宽度

#### 性别分布

**接口**: `GET /user/statistics/demographics/gender`

**图表类型**: 小型饼图

#### 年级分布

**接口**: `GET /user/statistics/demographics/grade`

**图表类型**: 小型柱状图（TOP 5）

#### 职位分布

**接口**: `GET /user/statistics/demographics/position`

**图表类型**: 小型环形图

#### 获奖类型分布

**接口**: `GET /user/statistics/awards/by-type`

**图表类型**: 小型饼图

---

## 技术实现建议

### 1. 图表库选择

**当前使用**: **Apache ECharts**

#### Apache ECharts
- **优点**: 
  - 功能强大，图表类型丰富
  - 中文文档完善，社区活跃
  - 支持高清渲染（devicePixelRatio）
  - 交互性强，动画流畅
  - 主题定制灵活
- **适用场景**: 复杂统计图表，数据可视化分析
- **安装**: `npm install echarts`

**关键配置**:
```typescript
// 使用 devicePixelRatio 修复高分辨率屏幕模糊问题
echarts.init(container, null, {
  devicePixelRatio: window.devicePixelRatio || 2
})
```

**迁移历史**: 
- ECharts → AntV G2 → Chart.js → ECharts
- 最终回到 ECharts，因为其清晰度和功能完整性

---

### 2. 组件结构

```typescript
// 统计卡片组件
components/
├── statistics/
│   ├── StatCard.tsx              // 数字卡片
│   ├── DepartmentChart.tsx       // 部门分布图
│   ├── PositionChart.tsx         // 职位分布图
│   ├── GenderChart.tsx           // 性别分布图
│   ├── GradeChart.tsx            // 年级分布图
│   ├── MajorChart.tsx            // 专业分布图
│   ├── MembershipTrendChart.tsx  // 入会趋势图
│   ├── AwardLevelChart.tsx       // 获奖等级分布图
│   ├── AwardTypeChart.tsx        // 获奖类型分布图
│   ├── AwardTrendChart.tsx       // 获奖趋势图
│   └── AwardRankingList.tsx      // 获奖排行榜

// 页面组件
pages/
├── dashboard/
│   └── index.tsx                 // 综合仪表盘
├── members/
│   ├── list.tsx                  // 成员列表
│   └── statistics.tsx            // 成员统计
└── awards/
    ├── list.tsx                  // 获奖列表
    └── statistics.tsx            // 获奖统计
```

---

### 3. 交互功能

#### 下钻功能
点击图表元素跳转到详细列表：
- 点击"技术部" → 跳转到该部门的成员列表
- 点击"国家级" → 跳转到国家级获奖列表
- 点击排行榜成员 → 跳转到该成员的获奖详情

**实现示例**:
```typescript
const handleChartClick = (params: any) => {
  const departmentId = params.data.departmentId
  router.push(`/members/list?departmentId=${departmentId}`)
}
```

#### Tooltip
鼠标悬停显示详细数据：
- 显示具体数值
- 显示百分比
- 显示额外信息

#### 图例切换
点击图例隐藏/显示某个数据系列

#### 数据导出
支持导出为：
- 图片（PNG/JPG）
- Excel表格
- PDF报告

---

### 4. 响应式设计

#### 桌面端（>= 1200px）
- 使用上述完整布局
- 多列并排显示

#### 平板端（768px - 1199px）
- 2列布局
- 部分图表缩小

#### 移动端（< 768px）
- 单列布局
- 图表简化为卡片+数字
- 复杂图表改为简单列表

**实现示例**:
```typescript
const isMobile = useMediaQuery('(max-width: 768px)')

return isMobile ? (
  <StatCard data={data} />
) : (
  <Chart data={data} />
)
```

---

### 5. 性能优化

#### 懒加载
图表组件按需加载：
```typescript
const AwardTrendChart = lazy(() => import('@/components/statistics/AwardTrendChart'))
```

#### 缓存策略
统计数据缓存5-10分钟：
```typescript
const { data, isLoading } = useQuery({
  queryKey: ['memberOverview'],
  queryFn: () => userApi.getMemberOverview(),
  staleTime: 5 * 60 * 1000, // 5分钟
  cacheTime: 10 * 60 * 1000, // 10分钟
})
```

#### 骨架屏
加载时显示占位符：
```typescript
{isLoading ? <ChartSkeleton /> : <Chart data={data} />}
```

#### 虚拟滚动
排行榜数据量大时使用虚拟滚动：
```typescript
import { FixedSizeList } from 'react-window'
```

---

### 6. 时间选择器

所有趋势图统一提供时间选择功能：

#### 快捷选项
- 最近7天
- 最近30天
- 最近3个月
- 最近6个月
- 最近12个月
- 本年
- 去年

#### 自定义范围
日期范围选择器（DateRangePicker）

#### 周期切换
- 按日
- 按月
- 按年

**实现示例**:
```typescript
const [timeRange, setTimeRange] = useState({
  period: 'month' as 'month' | 'year',
  limit: 12
})

const { data } = useQuery({
  queryKey: ['awardTrend', timeRange],
  queryFn: () => userApi.getAwardTrend(timeRange.period, timeRange.limit)
})
```

---

### 7. 空状态处理

无数据时显示友好的空状态：

```typescript
{data.length === 0 ? (
  <Empty
    image="/images/empty-chart.svg"
    description="暂无数据"
    action={
      <Button type="primary" onClick={handleAddData}>
        添加数据
      </Button>
    }
  />
) : (
  <Chart data={data} />
)}
```

---

## 开发优先级

### Phase 1（MVP - 核心功能）

**目标**: 快速上线基础统计功能

**时间**: 1-2周

**内容**:
1. ✅ 综合仪表盘首页
   - 快速概览卡片
   - 成员入会趋势图
   - 获奖趋势图
   
2. ✅ 成员统计页
   - 成员概览卡片
   - 部门分布图
   - 入会趋势图
   
3. ✅ 获奖统计页
   - 获奖概览卡片
   - 获奖排行榜
   - 获奖趋势图

**技术栈**:
- React + TypeScript
- ECharts
- React Query（数据缓存）
- Ant Design（UI组件）

---

### Phase 2（增强功能）

**目标**: 完善统计维度和交互

**时间**: 2-3周

**内容**:
1. ✅ 人口统计详细分析
   - 性别分布图
   - 年级分布图
   - 专业分布图
   
2. ✅ 获奖分布分析
   - 按等级分布图
   - 按类型分布图
   
3. ✅ 职位分布图

4. ✅ 图表交互功能
   - 下钻功能（点击跳转）
   - 筛选功能
   - Tooltip详情

5. ✅ 时间选择器
   - 快捷选项
   - 自定义范围
   - 周期切换

---

### Phase 3（优化提升）

**目标**: 提升用户体验和性能

**时间**: 1-2周

**内容**:
1. ✅ 数据导出功能
   - 导出为图片
   - 导出为Excel
   - 导出为PDF报告
   
2. ✅ 响应式优化
   - 移动端适配
   - 平板端适配
   
3. ✅ 性能优化
   - 懒加载
   - 虚拟滚动
   - 数据缓存
   
4. ✅ 数据对比功能
   - 同比对比
   - 环比对比
   
5. ✅ 实时数据更新
   - WebSocket推送
   - 自动刷新

---

### Phase 4（高级功能）

**目标**: 提供高级分析能力

**时间**: 2-3周

**内容**:
1. ✅ 自定义仪表盘
   - 拖拽布局
   - 自定义图表
   - 保存配置
   
2. ✅ 数据预测
   - 趋势预测
   - 异常检测
   
3. ✅ 报表订阅
   - 定期邮件报告
   - 数据告警
   
4. ✅ 多维度分析
   - 交叉分析
   - 钻取分析

---

## 附录

### A. 接口映射表

| 页面 | 图表 | 接口 | 方法 |
|------|------|------|------|
| 仪表盘 | 成员概览卡片 | `/user/statistics/overview` | GET |
| 仪表盘 | 获奖概览卡片 | `/user/statistics/awards/overview` | GET |
| 仪表盘 | 成员入会趋势 | `/user/statistics/membership/trend` | GET |
| 仪表盘 | 获奖趋势 | `/user/statistics/awards/trend` | GET |
| 仪表盘 | 部门分布 | `/user/statistics/departments` | GET |
| 仪表盘 | 获奖等级分布 | `/user/statistics/awards/by-level` | GET |
| 仪表盘 | 获奖排行榜 | `/user/statistics/awards/top` | GET |
| 成员统计 | 成员概览卡片 | `/user/statistics/overview` | GET |
| 成员统计 | 部门分布 | `/user/statistics/departments` | GET |
| 成员统计 | 职位分布 | `/user/statistics/positions` | GET |
| 成员统计 | 性别分布 | `/user/statistics/demographics/gender` | GET |
| 成员统计 | 年级分布 | `/user/statistics/demographics/grade` | GET |
| 成员统计 | 专业分布 | `/user/statistics/demographics/major` | GET |
| 成员统计 | 入会趋势 | `/user/statistics/membership/trend` | GET |
| 获奖统计 | 获奖概览卡片 | `/user/statistics/awards/overview` | GET |
| 获奖统计 | 按等级分布 | `/user/statistics/awards/by-level` | GET |
| 获奖统计 | 按类型分布 | `/user/statistics/awards/by-type` | GET |
| 获奖统计 | 获奖趋势 | `/user/statistics/awards/trend` | GET |
| 获奖统计 | 获奖排行榜 | `/user/statistics/awards/top` | GET |

---

### B. 图表类型参考

| 图表类型 | 适用场景 | ECharts 实现 |
|----------|----------|--------------|
| 数字卡片 | 单一指标展示 | 自定义组件 |
| 柱状图 | 分类数据对比 | `type: 'bar'` |
| 横向条形图 | 排名、TOP N | `type: 'bar'` + 交换 x/y 轴 |
| 饼图 | 占比分析 | `type: 'pie'` |
| 环形图 | 占比分析（带中心文字） | `type: 'pie'` + `radius: ['45%', '70%']` |
| 折线图 | 趋势分析 | `type: 'line'` |
| 面积图 | 趋势分析（强调累积） | `type: 'line'` + `areaStyle` |
| 分组柱状图 | 多系列对比 | 多个 `series` |
| 折线+面积组合 | 趋势分析（双重强调） | `type: 'line'` + `areaStyle` + `smooth` |

---

### C. 颜色规范

#### 主题色
- 主色：`#1890ff` (蓝色)
- 成功色：`#52c41a` (绿色)
- 警告色：`#faad14` (橙色)
- 错误色：`#f5222d` (红色)

#### 图表配色
- 部门分布：`['#1890ff', '#52c41a', '#faad14']` (总人数、正式成员、候选成员)
- 获奖等级：`['#FFD700', '#C0C0C0', '#CD7F32']` (金银铜)
- 性别分布：`['#1890ff', '#eb2f96', '#d9d9d9']` (男女未知)
- 趋势图：`['#52c41a', '#1890ff']` (转正人数、候选入会人数)
- 获奖趋势：`#667eea` (渐变色)

---

### D. 参考资源

- [ECharts 官方文档](https://echarts.apache.org/zh/index.html)
- [ECharts 配置项手册](https://echarts.apache.org/zh/option.html)
- [ECharts 示例](https://echarts.apache.org/examples/zh/index.html)
- [Vue Router 文档](https://router.vuejs.org/)

---

**文档维护**: 请在实现过程中及时更新本文档

**最后更新**: 2024-11-17
