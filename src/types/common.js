// Common utility types (JSDoc definitions for JavaScript)

/**
 * @typedef {Object} Result
 * @property {number} code - Response code
 * @property {string} message - Response message
 * @property {*} data - Response data
 */

/**
 * @typedef {Object} PageDTO
 * @property {number} pageNum - Current page number
 * @property {number} pageSize - Page size
 */

/**
 * @typedef {Object} PageVO
 * @property {Array} records - Page records
 * @property {number} total - Total count
 * @property {number} pageNum - Current page number
 * @property {number} pageSize - Page size
 */

/**
 * @typedef {'light' | 'dark'} ThemeMode
 */

export default {}
